# Screenshot assets for README

Place PNG captures in this folder using the filenames in the root **Screenshot checklist** table.

Minimum set for a polished README:

1. `screenshot_main.png` — full results view (Compound Failure or Pitot Drift)
2. `screenshot_landing.png` — UI before first run
3. `screenshot_incidents.png` — incident panel

`architecture.svg` is already committed (pipeline diagram).

Optional: replace `mission_control.png` if you keep that path elsewhere; the new README uses `screenshot_*.png` names.
